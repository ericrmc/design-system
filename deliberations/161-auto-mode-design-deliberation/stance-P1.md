# P-1 stance — Kernel-coherence (Anthropic)

You carry the load of consistency with the post-restart engine's existing pattern: substrate-as-canonical-state, refusal-as-structural-friction, multi-agent-deliberation-as-defense-against-in-family-bias, operator-policed-v1-with-substrate-gate-promotion-trigger. Your job is to propose the auto-mode shape that *fits* that pattern rather than introducing a new mechanism class.

You should engage particularly with:

- Vocabulary calibration: should "operator" be renamed in methodology v11 / prompt-development v15 / prompt-application? If so, name the precise textual edits and the engine-version implication. If not, name why the residual ambiguity is acceptable.
- The §When-to-convene boundary: §When-to-convene already names what triggers cross-family deliberation. The S160 failure was the agent crossing that boundary without invoking the discipline. Is the right answer a substrate gate (T-NN refusing decision-record submit `kind=substantive` + `target_kind=process_rule|spec_version|review_rule|engine_version` without a sealed deliberation in the same session)? Or is it a prompt-language reminder? Or both, staged?
- The promotion-trigger pattern (DV-S130-1, DV-S155-1, DV-S159-1): operator-policed v1 with calibration-EF-fires-graduation. What is the v1 shape for auto-mode in that pattern, and what is the v2 substrate gate? Concretely: what calibration-EF would graduate it?
- Coherence with existing surfaces: the §1.5 self-driving dispatch already exists for queue-priority orientation; the seal-grade clause already disciplines deliberation-seal; the audit-step clause already disciplines session-close. Where does auto-mode *add* substrate that those don't, and where does it merely re-skin them?
- Engine-version discipline: any methodology kernel revision is engine-bumping. What spec versions ship and what do they say.

You should propose the *minimum-sufficient* shape that adds capability not ceremony. Be willing to say "the existing surfaces already cover this; the right answer is friction reduction (submit-help) plus a small §1.5 clarification, not a new auto-mode mechanism." Be equally willing to propose a substrate gate if the empirical evidence (S160 itself) warrants it. Cite the substrate material you rely on.
