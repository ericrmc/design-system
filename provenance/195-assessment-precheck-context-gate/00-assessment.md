---
session: 195
title: assessment-precheck-context-gate — assessment
engine_version_at_open: engine-v52
mode: self-development
generated_by: selvedge export
---

# Assessment

## State at open

S195 opens kind=coding to ship assessment-submit precheck gate per operator-named-mandate at S194-close: relocate context-surfacing from orient to assessment-time as substrate-enforced T-NN refusal.

## Agenda

1. Operator-named-mandate at S194-close: wait-for-evidence approach precluded after 194 sessions of evidence that agents avoid the substrate; ship the gate now.
2. orient was wrong location (S194 mistake): agents skim orient under context pressure; relevant_history_anchored will be removed and surfaced at assessment-time instead.
3. Substrate-presented (not operator-curated): substrate is not frictionless; agents tend to avoid using it; auto-generated context-pack mandatory at assessment-submit.
4. One-shot mandatory per session: bin/selvedge context emits markdown pack + writes assessment_prechecks receipt + emits nonce; assessment-submit refuses without nonce.
5. Mirrors T-33 decision-record precheck shape (engine-v49) but at assessment-submit; codex-shape-consult per EF-S195-1 endorsed bootstrap-by-ordering + all-kinds-gate (no kind=meta exemption) + sha256-over-rendered-pack + atomic-consume.
6. Migration 047 creates assessment_prechecks table mirroring decision_prechecks shape with role_write_capabilities INSERT inline (lesson from S194 split-out 045+046).
7. Default no-target context behavior (substrate-presented floor): undisposed FRs + open HIGH OIs feed the pack when agent does not name targets; named --target narrows.
8. Bootstrap: S195 own assessment-submit happens before migration applies (open + assessment + implement + migrate + DV-S195-1 + close); gate fires from S196 onward; no hardcoded session-id exemption per codex caution.
9. orient.py cleanup: remove relevant_history_anchored section + 2 tests; engine_feedback_anchors table + T-37 + engine-feedback-anchor submit kind STAY (typed graph remains useful at assessment-precheck query time).
10. No multi-perspective deliberation: operator-named-mandate concretizing prior synthesis per §1.5 admissibility; codex-shape-consult sufficient for non-Anthropic-family read on shape.
