---
session: 176
title: anchor-trace-substrate-gate — deliberation
generated_by: selvedge export
---

# Deliberation

## D-27 — Anchor-trace substrate-gate shape: handler dispatch + evidence rows + T-NN refusal; operator excluded subtraction stance.

sealed_at: 2026-05-02T04:13:06.253Z

### P-1 (anthropic)

**Position.** Land migration 031 adding decision_chain_walks evidence rows; submit handler walks every cited alias in-band, attaches receipts, T-32 refuses on unresolved alias or zero-row receipt; v21 clause mandatory; engine v47 to v48

**schema_sketch.**
- Add `decision_chain_walks(chain_walk_id PK, decision_v2_id FK, anchor_alias TEXT, max_depth INT, walker_version TEXT, edge_count INT, result_atom_id FK text_atoms, created_at TIMESTAMP)`
- Anchor-alias FK by alias-resolve at insert; reject NULL anchor_alias and reject duplicates per (decision_v2_id, anchor_alias)
- result_atom_id stores the rendered markdown body via existing 8-240 atom path? No — atom cap is too small; use `result_text` LONGTEXT column directly, sha256 stored alongside for verifiability
- Add T-32 trigger: refuse decision_v2 INSERT when no decision_chain_walks rows match the cited-alias set the handler computed
**cli_surface.**
- Modify `selvedge/submit/decisions.py::_submit_decision_record` to compute cited-alias set from payload and dispatch chain-walk per alias via existing `selvedge.export.provenance` walker in-process
- Insert decision_chain_walks rows before T-32 fires
- Add `bin/selvedge query "SELECT decision_v2_id, anchor_alias, edge_count FROM decision_chain_walks ..."` for inspection (no new CLI subcommand needed)
- Keep `bin/selvedge export --provenance --print` pure-read for tactical use outside submit context
**migration_path.**
- Forward-only migration 031 creates tables + T-32 trigger; existing decision_v2 rows are grandfathered (T-32 fires on INSERT only, not retroactive validation)
- The migration’s own decision-record (DV-S176-1) is itself the first row under the gate; chain-walks dispatch on its supports + alternatives.rejections cites
- No backfill of historical decisions; FR-S173-3 watch becomes structurally moot once gate lands (every accepted record from S176+ has receipts)
**what_not.**
- Do not predicate-detect invocation points (the four (a)-(d) shapes from S175) at handler-time; walk every cited alias unconditionally — simpler, broader coverage, no false-negative on edge cases the predicates miss
- Do not store markdown transcript as primary proof object; the row count + edge_count + sha256 of result_text is the substrate-verifiable proof. (P-2 cross-family alignment.)
- Do not mutate substrate from `export --provenance --print` read path; the submit handler dispatches receipts, not the read tool. (P-2 cross-family alignment.)
**risk.**
- Cycle in cite-graph (DV cites EF cites DV) requires walker to track visited-set; existing walker may or may not — verify before ship
- Performance: a decision-record with 5 supports each citing distinct DVs at depth 3 dispatches 5 walks per submit; budget acceptable at engine-v47 throughput but cap at 10 walks per submit as guardrail
- Walker version drift: walker_version column lets future readers tell whether a receipt was generated by the current logic; no cross-version verification yet
**open_question.**
- Should perspective-alias cites (P-N-P-K) trigger chain-walk into deliberation graph, or stop at the perspective row? Initial ship: stop at perspective; followup if deliberation-graph-walk surfaces value
**what_lost.**
- I do not propose the cross-family P-2 obligation-ledger predicate-detection shape; preferring walk-everything simplicity over predicate-precision in the first ship
- I do not propose making the receipt graph_hash semantically-aware of cited-row content changes; immutable-snapshot at submit-time is sufficient for the audit-warrant the gate provides
- I do not propose deferring to S177; operator named the mandate at S176-open and the substrate change is small enough to ship in one session

### P-2 (openai)

**Position.** Ship a T-NN-backed provenance obligation ledger: submit path deterministically derives required chain-walk anchors, records walks as typed rows in same write_tx, SQL refuses unless matching receipts exist

**schema_sketch.**
- Add `provenance_walk_receipts`: `id`, `subject_kind`, `subject_alias`, `anchor_alias`, `max_depth`, `walker_version`, `graph_hash`, `created_at`, `invocation_point`
- Add unique key on `subject_alias`, `anchor_alias`, `invocation_point`, `walker_version` to make duplicate submits idempotent
- Add `provenance_walk_edges`: `receipt_id`, `edge_kind`, `from_alias`, `to_alias`, `depth`, `source_table`, `source_row_id`
- Add SQL triggers for the four S175 invocation predicates; refusal text names the missing anchor and invocation point
- Store `graph_hash` over ordered walked edges, not markdown text, so the receipt is substrate-verifiable and export formatting can change
**cli_surface.**
- Change decision submit handler to derive obligations before insert, call the existing provenance walker in-process, insert receipts and edge rows in the same `write_tx`
- Keep `export --provenance --print` pure-read by default; add `--record-for <subject_alias> --invocation-point <name>` only for explicit calibration or repair
- Add `bin/selvedge provenance obligations --decision-json <path>` to show which anchors would gate a draft before submit
- Add `bin/selvedge export --provenance --receipt <id> --print` to render the recorded walk actually used at submit time
**migration_path.**
- Forward-compatible migration: create tables and triggers without changing existing decision row shape
- Backfill nothing as passing evidence; historical decisions predate the gate and should not receive synthetic receipts
- Optionally backfill read-only diagnostic receipts with `subject_kind=historical_audit`, excluded from trigger satisfaction
- Gate only new S176+ writes; sealed S175 records remain valid but become visible precedent inputs for future walks
**what_not.**
- Do not make `export --provenance --print` always write rows; read commands should not mutate substrate or create accidental compliance evidence
- Do not accept a markdown transcript as proof; the enforceable object is the ordered edge set plus hash
- Do not rely on prompt wording, checklist fields, or self-attestation; those reproduce the S175 failure
- Do not defer to recurrence-EF graduation; FR-S173-5 depends on observing slips, while the operator asked for deterministic prevention now
- Do not use DV-S109-1 ceremony-subtraction as rejection basis; it is operator-precluded for D-27
**risk.**
- Walker drift can invalidate confidence in old receipts; include `walker_version` and render old receipts with their original version
- Predicate drift can cause false refusals; keep invocation point names explicit and test each of the four S175 shapes
- Hashing only edges may miss semantic changes in cited rows; this is acceptable for walk completion, but not for precedent interpretation
- Submit latency increases with graph depth; cap `max_depth` per invocation point and record truncation status if reached
**open_question.**
- Decide whether T-NN should require the agent to cite receipt IDs in the decision body, or whether substrate linkage by `subject_alias` is sufficient
**what_lost.**
- I do not take the maximal stance of forcing every support cite through provenance; that would create broad friction before S175 has proven the narrower predicates
- I do not take the export-side-effect stance; it is tempting, but an Anthropic-default reasoner may underweight how read-side mutation pollutes audit evidence
- I do not require automated semantic comparison against rejected alternatives yet; S176 should first make the chain walk observable and unskippable

### P-3 (anthropic)

**Position.** Adversarial-implementation stance: substrate-gate ships but implementation must defend against five concrete failure modes the operator-named mandate alone does not specify

**schema_sketch.**
- decision_chain_walks needs a `truncation_status` column (none/depth_capped/cycle_break/walker_error) so a receipt with edge_count=0 is distinguishable from a receipt the walker abandoned mid-traversal
- Add `walked_alias_set` JSON column or a `decision_chain_walk_visited(chain_walk_id, alias)` table for cycle detection across the receipt; without it a DV cites EF cites DV cycle either infinite-loops or silently truncates
- Schema must permit zero receipts when the decision has no cited aliases (e.g. an initial spec-version with no supersedes); T-32 refusal predicate must be cited_alias_count > 0 AND receipt_count < cited_alias_count, not receipt_count = 0
**cli_surface.**
- Submit handler must compute cited-alias set deterministically and idempotently; running the same payload twice must produce the same alias set or replay-debugging breaks
- Walker dispatch must run inside a single sqlite write_tx; partial commit on walker exception leaves orphaned receipt rows and a non-inserted decision_v2 row — atomicity is load-bearing for the gate correctness
- `bin/selvedge submit decision-record` exit code on T-32 refusal must name the unresolved alias and the missing receipt count; opaque T-32 refused text leaves operators guessing
**migration_path.**
- The migration own decision-record (DV-S176-1) cites DV-S173-1 + DV-S109-1 + DV-S174-1 + DV-S152-1 + EF-S173-1 + P-26-P-1 + P-27-P-1 + P-27-P-2 + P-27-P-3 + DV-S175-1 etc.
- Walker must succeed against this exact payload before T-32 lands or the gate first invocation refuses its own author
- Test the bootstrap explicitly: dry-run DV-S176-1 against the new handler before the migration applies, then apply migration, then submit DV-S176-1 — first-record-under-gate is the canonical hard case
**what_not.**
- Do not allow agent to bypass T-32 by citing fewer aliases; the gate must dispatch on the maximal alias set including supersedes-target and effects.target — trimming citations to escape the gate would re-create the recommended-clause failure
- Do not silently drop unresolved aliases from receipt set; refusal must enumerate every miss, not just the first
- Do not skip walker-version recording; future migrations changing walker logic need provenance
**risk.**
- Walker performance regression: depth=3 walks across 5-cite payloads multiply quickly; pytest must include a perf-floor test asserting submit completes inside reasonable budget
- Walker exception (e.g. existing DB corruption surfacing on a deep walk) crashes session-open first submit; failure mode must produce a useful refusal not a stack trace
- T-32 false-positive on legitimately-novel decisions: an initial spec-version (version=1) has no supersedes, no prior-decision rejections, no DV cites — predicate must permit zero receipts in that shape or initial specs cannot ship
- Bootstrap chicken-and-egg: T-32 cannot reference DV-S176-1 in its own refusal-text since DV-S176-1 doesn’t exist until after T-32 applies; refusal-text must be alias-agnostic
**open_question.**
- Does the chain-walk edge-set hash need to include cited-row content sha256, or only the topology of edges? P-2 raised this; the implementation choice affects whether subsequent edits to a cited row invalidate older receipts
**what_lost.**
- I do not take a ship-nothing stance; operator excluded subtraction
- I do not propose alternative substrate shapes; P-1 walk-everything and P-2 obligation-ledger cover the design space and my role is implementation-stress on whichever ships
- I do not propose extending the gate to non-decision submits (assessments, perspectives, close-records); scope discipline says limit to decision-record where re-proposal risk concentrates

### Synthesis

**D-27 synthesis — anchor-trace substrate-gate shape under operator-named-mandate.**

Three perspectives convened on shipping the substrate-gate that DV-S175-1 deferred. Operator at S176-open precluded DV-S109-1 ceremony-subtraction as rejection basis and named the recommended-clause-without-instrumentation pattern as the failure mode to repair. P-1 anthropic primary proposes walk-everything: handler dispatches chain-walk on every cited alias at decision-record submit-time. P-2 openai-codex cross-family proposes a tighter obligation-ledger keyed to predicate-detected invocation points (the four (a)-(d) shapes from S175). P-3 anthropic adversarial-implementation does not propose alternative shape; plays implementation-stress on whichever ships and surfaces five concrete defenses load-bearing for correctness.

**Convergences (independent multi-perspective agreement).**

**C-1 SHIP-GATE-NOW.** All three perspectives explicitly accept the substrate-gate ships at S176; operator-mandate precludes deferral and P-2 names this directly under What-not. Three-of-three unanimous.

**C-2 SUBMIT-HANDLER DISPATCH NOT EXPORT-SIDE-EFFECT.** P-1, P-2, P-3 converge: receipts are written by the decision-record submit handler in-band inside the same write_tx; the read-side `export --provenance --print` tool does NOT mutate substrate. P-2 is sharpest on this ("read commands should not mutate substrate or create accidental compliance evidence"); P-1 aligns; P-3 reinforces via atomicity argument. Cross-family contribution paying for itself.

**C-3 RECEIPTS AS SUBSTRATE ROWS LINKED TO DECISION_V2_ID.** All three converge on table-shape with at minimum (decision_v2_id FK, anchor_alias, walker_version, created_at) plus presentation columns. The proof object is the row + edge graph, not the rendered markdown.

**C-4 FORWARD-ONLY MIGRATION; NO HISTORICAL BACKFILL.** P-1 + P-2 explicitly; P-3 silent. Existing decision_v2 rows grandfathered; gate fires on S176+ INSERTs only.

**C-5 WALKER_VERSION RECORDED ON RECEIPTS.** P-1 + P-2 explicit; P-3 silent. Future walker logic changes do not invalidate prior receipts at read time.

**Divergences preserved.**

**D-1 WALK-SCOPE: walk-everything (P-1) vs predicate-detected obligations (P-2).** P-1 walks every cited alias unconditionally for simplicity + broader coverage ("walk-everything... no false-negative on edge cases the predicates miss"). P-2 derives obligations from the four S175 invocation predicates ("would create broad friction before S175 has proven the narrower predicates"). P-3 implementation-stress applies to either. The synthesis adopts P-1 walk-everything: every supports.cite ∪ alternatives.rejections.cite ∪ supersedes-target ∪ effects.target alias triggers a walk. Reasoning: predicate-detection at handler-time introduces a new failure mode (a predicate misses an invocation shape), and the recommended-clause backfire just demonstrated that prose-defined predicates leak. Walk-everything is uniformly defensible; cost is bounded by max-walks-per-submit cap. P-2's stance preserved as M-1 minority graduation path: if walk-everything proves over-broad in practice (calibration-EF naming receipt-noise), narrow to predicate-detection in v2.

**D-2 GRAPH_HASH CONTENT: edges-only (P-2) vs edges-plus-cited-row-content (P-3 open question).** P-2 advocates ordered-edge-set sha256; P-3 raises whether cited-row content sha256 should fold in. Synthesis adopts edges-only at v48: the receipt is a snapshot of WHO was cited at submit-time, not WHAT they said; cited-row mutation is rare (rows are append-mostly) and audit-readers consult the receipt against the live row deliberately. D-2 preserved: if a calibration-EF surfaces stale-cite invalidation pressure, content-hash extension is the v2 forward.

**Adversarial-implementation contributions adopted (all from P-3).**

- Cycle detection via walker visited-set; truncation_status enum (none/depth_capped/cycle_break/walker_error) records walker-state.
- Atomicity inside single write_tx; partial commit on walker exception is unacceptable; rollback the whole submit.
- Refusal text enumerates every unresolved alias + missing receipt count; opaque "T-32 refused" text is not admissible.
- Permit zero receipts when zero cited aliases (initial spec-version, version=1, no supersedes); T-32 predicate is `cited_alias_count > 0 AND receipt_count < cited_alias_count`, not `receipt_count = 0`.
- Bootstrap explicitly verified: DV-S176-1 (this session's substantive supersede) cites 8+ aliases including P-27-P-1/P-2/P-3 perspectives, DV-S175-1, DV-S173-1, DV-S109-1 (operator-precluded but admissible as cite-anchor for grounding), DV-S152-1, DV-S174-1, EF-S173-1, OI-S176-1; first-record-under-gate must succeed against own author at commit.

**Decision frame for DV-S176-1.**

Adopt: migration 031 creates `decision_chain_walks(chain_walk_id PK, decision_v2_id FK, anchor_alias TEXT, max_depth INT DEFAULT 3, walker_version TEXT, edge_count INT, truncation_status TEXT CHECK enum, result_text TEXT, result_sha256 TEXT, created_at TIMESTAMP, UNIQUE(decision_v2_id, anchor_alias))`. Handler `_submit_decision_record` computes cited alias union, dispatches walker in-band, inserts receipt rows, refuses E_REFUSAL_T32 on any walk failure or count mismatch. T-32 SQL trigger as defensive against direct-INSERT bypass per T-28 precedent. Pytest covers: walk-everything happy-path, unresolved-alias refusal, cycle detection, depth-cap truncation, perf-floor (5 cites at depth 3 within budget), zero-cite admit, bootstrap (DV-S176-1 self-test). Prompt-development v20 to v21 amends §5: clause becomes mandatory; substrate refuses on receipt absence; promotion-trigger paragraph removed since gate landed. Engine v47 to v48. FR-S173-3 calibration-watch becomes structurally moot (every accepted record from S176+ has receipts; tactical invocation count is no longer the watched variable). FR-S173-5 promotion-trigger fires by-mechanism via this session. OI-S176-1 closed by DV-S176-1.

Reject: D-1 P-2 predicate-detection shape (preserved as M-1 minority for v2 graduation if walk-everything is calibration-EF'd as over-broad), D-2 cited-row-content hash extension (preserved as v2 forward conditional on stale-cite recurrence), defer-to-S177 (operator-mandate excludes), prose-amendment-only (operator-mandate excludes), export-side-effect substrate-mutation (P-2 cross-family rebuttal load-bearing).

**Single-frame counterfactuals (per DV-S159-1 §4 seal-grade).**

1. Apply gate to perspective + assessment + close-record submits beyond decision-record. Position no perspective took as primary stance; P-3 explicitly named scope-limit ("re-proposal risk concentrates at decision-record"). Disposition: nilled-by-exclusion (taken-by-perspective P-3 What-not bullet; scope-limit explicit).
2. Use git-committed provenance file rather than runtime walker (substrate has SQLite as authoritative; git-side rendering is presentation). Disposition: nilled-by-exclusion (foreclosed by deliberation scope at convening time, named "substrate-gate" not "git-side").
3. Pre-submit lint at draft-time rather than commit-refusal (operator-policed not substrate-deterministic). Disposition: nilled-by-exclusion (operator-mandate excludes operator-policed solutions explicitly per "Use the database!" framing).

**Minorities preserved.**

**M-1 P-2 OBLIGATION-LEDGER.** P-2's predicate-detection shape is operationally narrower and avoids walk-everything's false-positive risk on noisy-cite payloads. Synthesis declined now on grounds the recommended-clause's failure was prose-defined predicates leaking; substrate-walking the maximal cite set is uniformly safer first-ship. M-1 is the natural graduation if calibration-EF surfaces walk-noise pressure.

**[synth] residual.** P-3 adversarial-implementation explicitly did not propose an alternative shape; the role was implementation-stress on the chosen design. The synthesis adopts every P-3 defense as part of the contract because operator-mandate "deterministic not prose" applies equally to defense-against-failure-modes as to gate-existence. Cross-family P-2 contribution on read/write-separation is the load-bearing protection against the obvious-but-wrong alternative (mutating from `export --print`); without P-2 the Anthropic-default sketch would have shipped that.


### Synthesis points

- **convergence C-1.** Ship substrate-gate at S176; deferral operator-precluded; three-of-three unanimous.
- **convergence C-2.** Submit-handler dispatches receipts in-band; export --print stays pure-read.
- **convergence C-3.** Receipts as substrate rows linked to decision_v2_id; row + edges are proof object not markdown.
- **convergence C-4.** Forward-only migration; existing decision_v2 rows grandfathered.
- **convergence C-5.** walker_version recorded on each receipt for forward-compatibility.
- **divergence D-1.** Walk-scope: walk-everything (P-1 adopted) vs predicate-detected obligations (P-2 minority).
- **divergence D-2.** Graph-hash content: edges-only (P-2 adopted) vs cited-row-content sha256 (P-3 open question deferred to v2).
- **minority M-1.** P-2 obligation-ledger predicate-detection; preserved as v2 graduation if walk-everything proves over-broad via calibration-EF.
