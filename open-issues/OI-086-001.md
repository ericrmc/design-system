---
id: OI-086-001
status: open
priority: MEDIUM
surfaced-in-session: 86
generated_by: selvedge export --issues
---

# spec_clause source_decision_v2_id traceability is unenforced (and unused)

All 272 spec_clauses rows have source_decision_v2_id IS NULL; the column is documentation, not a constraint.

All 272 rows in spec_clauses have source_decision_v2_id IS NULL. The traceability column was named in the S082 P-1 schema sketch as source_decision_id fk not null, but the substrate landed it nullable to admit legacy-imported clauses. No clause — including those that pass through the post-restart write path — populates the column. The column is documentation, not a constraint. This breaks the substrate-only-state contract at its load-bearing point: if specs are canonical (S084 D-2) and clauses must trace to decisions to be auditable, then a NULL source_decision_v2_id on a non-legacy clause is a silent provenance loss. What's needed: tighten the constraint on non-legacy paths — spec_clauses inserted via submit spec-clause should require source_decision_v2_id IS NOT NULL; legacy_imports remain exempt. Implementation: schema-level CHECK conditional on a legacy-marker column or an immutable insertion-mode column, plus dispatcher-side enforcement at submit time. Block on OI-085-001 (re-decompose v2 specs) so the post-restart write path has been exercised end-to-end before the constraint is tightened.

---
*Substrate-canonical: regenerated from `issues` row by `selvedge export --issues`. Body atoms are capped at 4000 chars; if this issue predates engine-v22 (pre-S088) the body is a compressed canonical summary and the verbatim pre-substrate prose is in git history.*
