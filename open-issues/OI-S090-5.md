---
id: OI-S090-5
status: open
priority: MEDIUM
surfaced-in-session: 90
generated_by: selvedge export --issues
---

# Substrate-driven spec content authoring path (markdown body editing remains the only option)

After engine-v24 hook lands, updating spec body markdown still requires direct file write (Bash, since hook only guards Edit/Write). Need a substrate-driven authoring CLI.

S090 implemented OI-085-002 by wiring a PreToolUse hook on Edit/Write/MultiEdit/NotebookEdit. The hook refuses substrate-canonical markdown writes unless SELVEDGE_EXPORT_CONTEXT=1. However, when bumping engine-manifest from v23 to v24 in this same session, the body file (specifications/engine-manifest.md) still needed manual content authoring before submit spec-version could hash it. Bash file-write is outside the hook scope by design (matching all Bash would defeat scalability), but this means substrate-canonical spec content is still authored via direct markdown edit — the structural guarantee covers Claude tool surface, not the underlying authoring discipline. A future session should add a substrate-CLI authoring path (e.g., spec-clause-driven body materialiser, or a submit kind that takes inline content and writes the body file in-process under SELVEDGE_EXPORT_CONTEXT). Until then, spec body authoring uses Bash + spec-version submit, recorded in commit history.

---
*Substrate-canonical: regenerated from `issues` row by `selvedge export --issues`. Body atoms are capped at 4000 chars; if this issue predates engine-v22 (pre-S088) the body is a compressed canonical summary and the verbatim pre-substrate prose is in git history.*
