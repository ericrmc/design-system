---
id: OI-S091-1
status: open
priority: LOW
surfaced-in-session: 91
generated_by: selvedge export --issues
---

# Migration 011 encodes engine-v24 as a snapshot; future fresh workspaces start one or more versions stale

Migration 011 backfills current_engine_version=engine-v24 unconditionally; a fresh workspace replaying 001-011 after S091 starts at v24 until the first engine-manifest submit propagates.

S091 T-20 finding F2 (medium, adjudicated): the load-bearing structural fix is the handler-side metadata propagation in _submit_spec_version. Migration 011 is a one-time correction encoding engine-v24 as a snapshot. For a fresh workspace replaying 001-011 after further engine bumps in S092+, the metadata lands at engine-v24 until the first engine-manifest spec_version submit propagates the actual current value. This is acceptable per the adjudication (apply-once semantics + structural propagation) but a future session shipping the next engine bump should consider either (a) re-snapshotting in a new migration that brings the value current, or (b) refactoring to derive from spec_versions when those rows finally exist (which they do not at migrations-apply time). Touching schema_migrations.sha256 to rewrite applied migration content is rejected as it breaks the substrate audit trail.

---
*Substrate-canonical: regenerated from `issues` row by `selvedge export --issues`. Body atoms are capped at 4000 chars; if this issue predates engine-v22 (pre-S088) the body is a compressed canonical summary and the verbatim pre-substrate prose is in git history.*
