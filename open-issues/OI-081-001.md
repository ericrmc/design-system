---
id: OI-081-001
status: open
priority: LOW
surfaced-in-session: 81
generated_by: selvedge export --issues
---

# Should the table-count budget (078 D-10) be replaced by a derived-only check?

Minority position from S081 D-2: table-count budget is derived quantity masquerading as a constraint; deferred to post-release-gate review.

Source: minority position from S081 deliberation D-2 held by P-2-adversarial (Anthropic family, separate context, briefed adversarially); recorded as substrate synthesis_points.synthesis_point_id=5 (kind=minority). The table-count budget set by 078 D-10 (≤16 tables, breachable with cause) is a derived quantity masquerading as a constraint. The substrate can compute its own table count via SELECT COUNT(*) FROM sqlite_master; the validator already runs pre-commit. The protective mechanism that the prose-budget-plus-manifest-enumeration pair carries is narrative discipline about the count, and that mechanism produced two miscounts of two different tables across three sessions (079→080→081). Post-gate sessions should consider: (1) replace budget with selvedge validate --table-budget derived check; (2) remove manifest's prose enumeration; (3) keep D-10 if the human-readable budget number serves orienting/anchoring functions derived checks would not preserve. Cross-family non-Anthropic perspective on whether the budget as mechanism still earns its keep would be load-bearing.

---
*Substrate-canonical: regenerated from `issues` row by `selvedge export --issues`. Body atoms are capped at 4000 chars; if this issue predates engine-v22 (pre-S088) the body is a compressed canonical summary and the verbatim pre-substrate prose is in git history.*
